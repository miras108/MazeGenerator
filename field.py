# from abc import ABC, abstractmethod
# from point import Point
from maze import Point
#
# class Field(ABC):
#     @abstractmethod
#     def __init__(self, point: Point):
#         self.coordinates = point

class Field:
    def __init__(self, point: Point, dx):
        #self.area =
        pass